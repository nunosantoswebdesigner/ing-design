---
title: New Component
description: Replace this description with a one-line summary of what the component does.
---

import { NewComponent } from "@/registry/new-york/new-component";

<ComponentThemeTabs>
  <ThemeTab name="new-york">
    <PreviewWithCode name="new-component">
      <NewComponent className="p-6">New component</NewComponent>
    </PreviewWithCode>
  </ThemeTab>

  <ThemeTab name="force8">
    <PreviewWithCode name="new-component">
      <NewComponent className="p-6">New component</NewComponent>
    </PreviewWithCode>
  </ThemeTab>

  <ThemeTab name="myswissski">
    <PreviewWithCode name="new-component">
      <NewComponent className="p-6">New component</NewComponent>
    </PreviewWithCode>
  </ThemeTab>

  <ThemeTab name="myicehockey">
    <PreviewWithCode name="new-component">
      <NewComponent className="p-6">New component</NewComponent>
    </PreviewWithCode>
  </ThemeTab>
</ComponentThemeTabs>

## Variants

Add a description of each variant here.

<div className="flex flex-wrap gap-3 items-center my-6">
  <NewComponent className="p-4">Default</NewComponent>
</div>

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `className` | `string` | — | Additional CSS classes |
| `children` | `React.ReactNode` | — | Content inside the component |

## Usage

```tsx
import { NewComponent } from "@/components/ui/new-component";

<NewComponent>Content</NewComponent>
```

## Specs

```md
# New York — NewComponent Specs

## Component
- name: new-component
- file: registry/new-york/new-component.tsx
- installs to: components/ui/new-component.tsx
- dependencies: (none — add if needed, e.g. class-variance-authority, radix-ui)

## What is it
Replace this with a description of what the component does, when to use it,
and what problem it solves. Include any usage rules — e.g. "use X instead of Y
when the action changes state", "avoid using more than one per page", etc.

## Visual identity
Describe the visual character of this component in this theme.
- Shape/radius: (e.g. rounded-md, pill, square)
- Color role: (e.g. uses --primary for the active state)
- Weight: (e.g. medium presence, low visual noise)
- Key trait: (the one thing that makes it recognisable)

## Design tokens
--background: oklch(1 0 0)
--foreground: oklch(0.145 0 0)
--card: oklch(1 0 0)
--card-foreground: oklch(0.145 0 0)
--primary: oklch(0.205 0 0)
--primary-foreground: oklch(0.985 0 0)
--secondary: oklch(0.97 0 0)
--secondary-foreground: oklch(0.205 0 0)
--muted: oklch(0.97 0 0)
--muted-foreground: oklch(0.556 0 0)
--accent: oklch(0.97 0 0)
--accent-foreground: oklch(0.205 0 0)
--destructive: oklch(0.577 0.245 27.325)
--border: oklch(0.922 0 0)
--input: oklch(0.922 0 0)
--ring: oklch(0.708 0 0)
--radius: 0.625rem

## shadcn config (components.json)
- style: new-york
- rsc: true
- tsx: true
- baseColor: neutral
- cssVariables: true
- iconLibrary: lucide
- aliases:
  - components → @/components
  - ui → @/components/ui
  - utils → @/lib/utils
  - lib → @/lib
  - hooks → @/hooks

## Variants
- default → (describe the default appearance and use case)
- (add more variants as needed)

## States
- default:  (describe the resting appearance)
- hover:    (what changes on hover)
- focus:    (focus ring color and offset — always use --ring)
- active:   (pressed/active state if applicable)
- disabled: opacity-50, pointer-events-none
- loading:  (if applicable — Loader2Icon + animate-spin)
- empty:    (if the component has an empty state)
- error:    (if the component has an error state)

## Conventions
- Use cn() from @/lib/utils for all conditional classNames
- Props extend the native HTML element (e.g. HTMLDivElement)
- Tailwind CSS 4 utility classes only — no inline styles
- No external dependencies unless declared in registry.json
- One default variant per visual group maximum
- Spacing between adjacent instances: gap-2 (8px) minimum
- Interactive elements require aria-label when text is absent
```

## Installation

<CommandBox componentName="new-component" className="mt-2" />
